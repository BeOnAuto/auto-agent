{
  "name": "auto-agent",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "cd cli && pnpm build",
    "test": "cd cli && pnpm test",
    "prepare": "cd cli && pnpm install && pnpm build"
  }
}
